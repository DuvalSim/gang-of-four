name: publish

on:
  push:
    branches: ['main']

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.actor }}/gang-of-four-backend:latest

jobs:
  publish:
    name: publish image
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: login
        run: |
          echo ${{ secrets.GITHUB_TOKEN }} | docker login ghcr.io -u ${{ github.actor }} --password-stdin
      - name: Build and Publish
        run: |
          cd backend
          docker build . --tag ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          docker push ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}


  # deploy:
  #   needs: publish
  #   name: deploy image
  #   runs-on: ubuntu-latest

  #   steps:
  #     - name: install ssh keys
  #       run: |
  #         install -m 600 -D /dev/null ~/.ssh/id_rsa
  #         echo "${{ secrets.SSH_PRIVATE_KEY }}" > ~/.ssh/id_rsa
  #         ssh-keyscan -H ${{ secrets.SSH_HOST }} > ~/.ssh/known_hosts
  #     - name: connect and pull
  #       run: |
  #         ssh ${{ secrets.SSH_USER }}@${{ secrets.SSH_HOST }} "
  #           cd ${{ secrets.WORK_DIR }} && \
  #           docker pull ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }} && \
  #           docker stop gang-of-four-backend || true && \
  #           docker rm gang-of-four-backend || true && \
  #           docker run -d --restart=always --name gang-of-four-backend -p 8000:8000 ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
  #         "
  #     - name: cleanup
  #       run: rm -rf ~/.ssh